module.exports = {
    // Use semicolons at the end of statements
    semi: true,
  
    // Use single quotes instead of double quotes
    singleQuote: true,
  
    // Use trailing commas wherever possible (ES5+ syntax)
    trailingComma: 'all',
  
    // Add a space inside curly braces
    bracketSpacing: true,
  
    // Put the `>` of a multi-line JSX element at the end of the last line
    jsxBracketSameLine: false,
  
    // Use parentheses around a sole arrow function parameter
    arrowParens: 'always',
  
    // Set the maximum line length
    printWidth: 80,
  
    // Set the number of spaces per indentation level
    tabWidth: 4,
  
    // Use spaces instead of tabs
    useTabs: false,
  
    // Enforce consistent line endings (use "lf" for Linux/Mac or "auto" for cross-platform projects)
    endOfLine: 'lf',
  };
  